<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800;900&family=Dancing+Script:wght@400;700&display=swap" rel="stylesheet">
    
    <title>{{ product.title | default: 'BAGLUSH - Premium Gym Bags' }} | {{ shop.name }}</title>
    <meta name="description" content="{{ product.description | strip_html | truncate: 160 | default: 'Premium waterproof gym bags for the modern woman' }}" />
    <meta name="author" content="{{ shop.name }}" />
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="product" />
    <meta property="og:url" content="{{ request.origin }}{{ product.url }}" />
    <meta property="og:title" content="{{ product.title | default: 'BAGLUSH - Premium Gym Bags' }}" />
    <meta property="og:description" content="{{ product.description | strip_html | truncate: 160 | default: 'Premium waterproof gym bags for the modern woman' }}" />
    <meta property="og:image" content="{{ product.featured_image | img_url: '1200x630' | default: 'hero-lifestyle.jpg' | asset_url }}" />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:title" content="{{ product.title | default: 'BAGLUSH - Premium Gym Bags' }}" />
    <meta property="twitter:description" content="{{ product.description | strip_html | truncate: 160 | default: 'Premium waterproof gym bags for the modern woman' }}" />
    <meta property="twitter:image" content="{{ product.featured_image | img_url: '1200x630' | default: 'hero-lifestyle.jpg' | asset_url }}" />

    <!-- Shopify Required -->
    {{ content_for_header }}
    
    <!-- Canonical URL -->
    <link rel="canonical" href="{{ request.origin }}{{ product.url }}" />
    
    <!-- Structured Data -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org/",
      "@type": "Product",
      "name": "{{ product.title | json }}",
      "description": "{{ product.description | strip_html | json }}",
      "image": "{{ product.featured_image | img_url: 'master' }}",
      "brand": {
        "@type": "Brand",
        "name": "{{ shop.name | json }}"
      },
      "offers": {
        "@type": "Offer",
        "price": "{{ product.price | money_without_currency }}",
        "priceCurrency": "{{ cart.currency.iso_code }}",
        "availability": "{% if product.available %}https://schema.org/InStock{% else %}https://schema.org/OutOfStock{% endif %}",
        "url": "{{ request.origin }}{{ product.url }}"
      }
    }
    </script>

    {{ 'theme.css' | asset_url | stylesheet_tag }}
</head>
<body>
    <div id="root">
        <!-- Header -->
        <header class="header">
            <div class="container">
                <div class="header-content">
                    <h1 class="brand-title">{{ shop.name | default: 'BAGLUSH' }}</h1>
                    <p class="brand-subtitle">Premium Lifestyle</p>
                </div>
            </div>
        </header>

        <!-- Hero Section -->
        <section class="hero">
            <div class="hero-background"></div>
            <div class="container">
                <div class="hero-content">
                    <h1 class="hero-title">
                        {{ product.metafields.custom.hero_title | default: 'Your Perfect' }}
                        <span class="hero-title-accent">{{ product.metafields.custom.hero_accent | default: 'Workout Companion' }}</span>
                    </h1>
                    <p class="hero-description">
                        {{ product.metafields.custom.hero_description | default: 'Stylish, functional, and designed for the modern woman who demands both beauty and performance from her accessories.' }}
                    </p>
                    <div class="hero-actions">
                        <button class="btn btn-cta" onclick="scrollToSection('color-selection')">
                            <svg class="btn-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                                <path d="M6 2L3 6v14a2 2 0 002 2h14a2 2 0 002-2V6l-3-4z"/>
                                <line x1="3" y1="6" x2="21" y2="6"/>
                                <path d="m16 10l-4 4-4-4"/>
                            </svg>
                            Shop Now - {{ product.price | money }}
                        </button>
                    </div>
                </div>
            </div>
            <div class="scroll-indicator">
                <div class="scroll-mouse">
                    <div class="scroll-dot"></div>
                </div>
            </div>
        </section>

        <!-- Color Selection -->
        <section id="color-selection" class="section section-muted">
            <div class="container">
                <div class="section-header">
                    <h2 class="section-title">Choose Your Style</h2>
                    <p class="section-description">Express your personality with our curated palette of soft, dreamy colors</p>
                </div>
                
                <div class="color-selector">
                    <h3 class="color-selector-title">Choose Your Color</h3>
                    <div class="color-options" id="colorOptions">
                        {% for variant in product.variants %}
                            {% if variant.option1 contains 'Color' or variant.option2 contains 'Color' or variant.option3 contains 'Color' %}
                                <button class="color-option {% if forloop.first %}selected{% endif %}" 
                                        data-variant-id="{{ variant.id }}" 
                                        data-color="{{ variant.title }}"
                                        data-price="{{ variant.price | money }}"
                                        style="background-color: {{ variant.metafields.custom.color_hex | default: '#F8BBD9' }}">
                                    {% if forloop.first %}
                                        <svg class="color-checkmark" viewBox="0 0 20 20" fill="currentColor">
                                            <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                                        </svg>
                                    {% endif %}
                                </button>
                            {% endif %}
                        {% endfor %}
                    </div>
                    <p class="selected-color-name" id="selectedColorName">{{ product.variants.first.title | default: 'Pink' }}</p>
                </div>

                <!-- Add to Cart Form -->
                <form action="/cart/add" method="post" enctype="multipart/form-data" id="add-to-cart-form">
                    <select name="id" id="variant-selector" style="display: none;">
                        {% for variant in product.variants %}
                            <option value="{{ variant.id }}" {% if forloop.first %}selected{% endif %}>
                                {{ variant.title }} - {{ variant.price | money }}
                            </option>
                        {% endfor %}
                    </select>
                    
                    <div class="product-actions">
                        <button type="submit" class="btn btn-cta" {% unless product.available %}disabled{% endunless %}>
                            <svg class="btn-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                                <path d="M6 2L3 6v14a2 2 0 002 2h14a2 2 0 002-2V6l-3-4z"/>
                                <line x1="3" y1="6" x2="21" y2="6"/>
                                <path d="m16 10l-4 4-4-4"/>
                            </svg>
                            {% if product.available %}
                                Add to Cart - <span id="variant-price">{{ product.price | money }}</span>
                            {% else %}
                                Sold Out
                            {% endif %}
                        </button>
                    </div>
                </form>
            </div>
        </section>

        <!-- Features -->
        <section class="section">
            <div class="container">
                <div class="section-header">
                    <h2 class="section-title">Designed for Your Lifestyle</h2>
                    <p class="section-description">Every detail crafted with care to support your active, beautiful life</p>
                </div>
                
                <div class="features-grid">
                    <div class="feature-card">
                        <div class="feature-icon">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
                                <path d="M7 16.3c2.2 0 4-1.83 4-4.05 0-1.16-.57-2.26-1.71-3.19S7.29 6.75 7 5.3c-.29 1.45-1.14 2.84-2.29 3.76S3 11.19 3 12.25c0 2.22 1.8 4.05 4 4.05z"/>
                                <path d="M12.56 6.6A10.97 10.97 0 0 0 14 3.02c.5 2.5 2.04 4.6 4.14 5.78s3.14 2.35 3.14 3.9a5.01 5.01 0 0 1-5.02 5.02"/>
                            </svg>
                        </div>
                        <h3 class="feature-title">Waterproof Protection</h3>
                        <p class="feature-description">Premium water-resistant fabric keeps your essentials dry during any workout or weather.</p>
                    </div>

                    <div class="feature-card">
                        <div class="feature-icon">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
                                <path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"/>
                                <polyline points="3.27,6.96 12,12.01 20.73,6.96"/>
                                <line x1="12" y1="22.08" x2="12" y2="12"/>
                            </svg>
                        </div>
                        <h3 class="feature-title">Smart Compartments</h3>
                        <p class="feature-description">Thoughtfully designed spaces for shoes, clothes, water bottle, and personal items.</p>
                    </div>

                    <div class="feature-card">
                        <div class="feature-icon">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
                                <path d="M20.24 12.24a6 6 0 0 0-8.49-8.49L5 10.5V19h8.5z"/>
                                <line x1="16" y1="8" x2="2" y2="22"/>
                                <line x1="17.5" y1="15" x2="9" y2="15"/>
                            </svg>
                        </div>
                        <h3 class="feature-title">Lightweight Design</h3>
                        <p class="feature-description">Ultra-light materials that won't weigh you down while maintaining durability.</p>
                    </div>

                    <div class="feature-card">
                        <div class="feature-icon">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
                                <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
                            </svg>
                        </div>
                        <h3 class="feature-title">Lifetime Warranty</h3>
                        <p class="feature-description">We stand behind our quality with a comprehensive lifetime warranty on all bags.</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- Rest of the template continues with lifestyle images, reviews, Instagram gallery, FAQ, comments, CTA, contact, and footer sections... -->
        
    </div>

    {{ 'theme.js' | asset_url | script_tag }}
</body>
</html>
{% comment %} Product page {% endcomment %}
{{ product.title }}